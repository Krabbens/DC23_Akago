<!DOCTYPE html>
<html lang="pl">
    <head>
        <meta charset="utf-8">
        <title>{% block title %}{% endblock %} &ndash; Akago Systems Incorporated</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <script src="https://cdn.tailwindcss.com"></script>
        <style>
            body {
                background-image: url("/static/bg.jpg");
                background-size: cover;
                background-position: center;
                background-attachment: fixed;
            }
            body::before {
                content: "";
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0,0,0,0.5);
                z-index: -1;
            }
        </style>
        {% block head %}{% endblock %}
    </head>
    <body class="min-h-screen flex items-center justify-center bg-opacity-60">
        {% block content %}{% endblock %}
    </body>
</html>